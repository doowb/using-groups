<div class="col-sm-3 col-sm-offset-1 blog-sidebar">
  <div class="sidebar-module sidebar-module-inset">
    <h4>About</h4>
    {{#markdown}}
This is an example of using _permalinks_ and _groups_ to create _view collections_ from front-matter to render multiple pages that are dynamically built.
    {{/markdown}}
  </div>
  <div class="sidebar-module">
    <h4><a href="{{link-to (first (get "items" (categories)))}}">Categories</a></h4>
    <ol class="list-unstyled">
    {{#each (get "items" (categories))}}
      {{#if this.data.category}}
      <li><a href="{{link-to ..}}">{{../data.category}}</a></li>
      {{/if}}
    {{/each}}
    </ol>
  </div>
  <div class="sidebar-module">
    <h4><a href="{{link-to (first (get "items" (tags)))}}">Tags</a></h4>
    <ol class="list-unstyled">
    {{#each (get "items" (tags))}}
      {{#if this.data.tag}}
      <li><a href="{{link-to ..}}">{{../data.tag}}</a></li>
      {{/if}}
    {{/each}}
    </ol>
  </div>
  <div class="sidebar-module">
    <h4>Elsewhere</h4>
    <ol class="list-unstyled">
      <li><a href="#">GitHub</a></li>
      <li><a href="#">Twitter</a></li>
      <li><a href="#">Facebook</a></li>
    </ol>
  </div>
</div><!-- /.blog-sidebar -->
